Underline tab bar for switching between sections (Upcoming / Past appointments).

```jsx
<Tabs tabs={[{value:'upcoming',label:'Upcoming'},{value:'past',label:'Past'}]} active={tab} onChange={setTab} />
```
