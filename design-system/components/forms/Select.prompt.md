Native dropdown select, styled to match Input.

```jsx
<Select label="Service" options={[{value:'bath',label:'Bath & brush'},{value:'full',label:'Full groom'}]} value={service} onChange={setService} />
```
