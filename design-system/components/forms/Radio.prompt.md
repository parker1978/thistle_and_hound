Single-select radio dot, used in grouped option lists (service tiers, arrival windows).

```jsx
<Radio name="tier" label="Full groom — bath, cut, nails" checked={tier==='full'} onChange={()=>setTier('full')} />
```
