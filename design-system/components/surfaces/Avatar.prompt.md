Groomer/client circular photo, falling back to initials on a soft green tint. Intentional addition — needed to represent groomers and pets, not defined by any source.

```jsx
<Avatar name="Marisol Ortiz" src={groomer.photo} size={56} />
```
